apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-api-events
spec:
  selector:
    matchLabels:
      app: api-events
  replicas: 1
  template:
    metadata:
      labels:
        app: api-events
    spec:
      containers:
      - name: api-events
        image: 862980326144.dkr.ecr.us-east-1.amazonaws.com/api_events:dev-IMAGE_TAG
        resources:
          requests:
            memory: "128Mi"
            cpu: "125m"
        ports:
        - containerPort: 5015